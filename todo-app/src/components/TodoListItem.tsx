import React from 'react'

/**
 * @description Renders an HTML division element containing the necessary markup for
 * a single todo item.
 * 
 * @returns { HTML div element } a division element with the content "TodoListItem".
 * 
 * 		- `<div>` - A HTML div element representing a Todo List Item.
 */
const TodoListItem = () => {
  return (
    <div>TodoListItem</div>
  )
}

export default TodoListItem